package com.example.apexphotolab.working_project.tool_panel.eraser

/**
 * Job: Definitive Eraser Mode Registry.
 * Purified: Logic-only registry. 
 * NO UI DEPENDENCIES (Labels).
 */
enum class EraserMode {
    FREEFORM, PRECISION
}
