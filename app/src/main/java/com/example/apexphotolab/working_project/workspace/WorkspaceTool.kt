package com.example.apexphotolab.working_project.workspace

/**
 * Job: Definitive Tool Registry.
 * Purified: Logic-only registry of editor modes.
 * NO UI DEPENDENCIES (Icons, Labels).
 */
enum class WorkspaceTool {
    MOVE, ERASER, REMOVE_BG
}