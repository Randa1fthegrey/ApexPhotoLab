package com.example.apexphotolab.workspace.tool_panel.export.data

/**
 * A simple, shared enum to define the available export formats.
 * Updated: Professional Suite expansion.
 */
enum class ExportType { 
    PNG, SVG, JPG, 
    WEBP, BMP, 
    PSD, TIFF, XCF 
}